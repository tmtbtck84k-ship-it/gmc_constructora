<?php
/**
 * Plantilla de variables de entorno.
 * Copiar como application/config/.env.php y completar con valores reales.
 * NO COMMITEAR application/config/.env.php (está en .gitignore).
 */

return [

    // ENTORNO: development | testing | production
    'ENVIRONMENT' => 'development',

    // URL base SIN slash final
    'BASE_URL' => 'http://localhost:8000',

    // Base de datos
    'DB_HOSTNAME' => 'localhost',
    'DB_PORT'     => 3306,
    'DB_USERNAME' => 'gmc_user',
    'DB_PASSWORD' => 'cambiar_en_produccion',
    'DB_DATABASE' => 'gmc_erp',
    'DB_CHARSET'  => 'utf8mb4',
    'DB_COLLATE'  => 'utf8mb4_unicode_ci',

    // Llave de encripción (generar con: php -r 'echo bin2hex(random_bytes(32));')
    'ENCRYPTION_KEY' => 'CAMBIAR_GENERAR_64_HEX_CHARS',

    // Sesiones
    'SESSION_NAME'        => 'gmc_session',
    'SESSION_EXPIRATION'  => 7200,            // 2 horas
    'SESSION_COOKIE_SECURE' => false,         // true en producción HTTPS
    'SESSION_SAMESITE'    => 'Lax',

    // SMTP
    'SMTP_HOST'       => 'smtp.tu-proveedor.cl',
    'SMTP_PORT'       => 587,
    'SMTP_USERNAME'   => 'no-reply@gmc.cl',
    'SMTP_PASSWORD'   => 'cambiar_en_produccion',
    'SMTP_ENCRYPTION' => 'tls',               // tls | ssl
    'SMTP_FROM_NAME'  => 'ERP GMC',
    'SMTP_FROM_EMAIL' => 'no-reply@gmc.cl',

    // Uploads
    'UPLOAD_MAX_BYTES'  => 10 * 1024 * 1024,  // 10 MB
    'UPLOAD_BASE_PATH'  => __DIR__ . '/../../storage/uploads',
    'UPLOAD_ALLOWED_EXT'=> 'pdf,jpg,jpeg,png,doc,docx,xls,xlsx,csv,txt',

    // Rate limit login
    'LOGIN_MAX_ATTEMPTS'  => 5,
    'LOGIN_WINDOW_SECONDS'=> 900,             // 15 min ventana
    'LOGIN_LOCKOUT_SECONDS'=> 900,            // 15 min bloqueo

    // Token de recuperación de clave (segundos)
    'PASSWORD_RESET_TTL' => 3600,             // 60 min

    // Empresa
    'COMPANY_NAME' => 'Constructora GMC',
    'COMPANY_RUT'  => '00000000-0',

    // Backup
    'BACKUP_PATH' => '/var/backups/gmc',
    'BACKUP_RETENTION_DAYS' => 14,

    // Debug toolbar (solo development)
    'DEBUG_TOOLBAR' => true,
];
